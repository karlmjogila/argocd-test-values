# Dev-specific overrides

global:
  labels:
    env: dev

deployments:
  api:
    # Dev uses more replicas for testing
    minReplicas: 2
    maxReplicas: 2

    # Dev gets more resources
    resources:
      limits:
        cpu: 200m
        memory: 256Mi
